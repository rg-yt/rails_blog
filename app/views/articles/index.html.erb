<div>
  <h1 class="font-bold text-4xl">Hello, Rails!</h1>
  
  <ul>
    <% @articles.each do |article| %>
      <li><%= link_to article.title, article %></li>
    <% end %>  
  </ul>

  <%= link_to 'New Article', new_article_path %>
</div>

